package main

import (
	"fmt"
)

func main() {

	name := "docker"

	switch name {
	case "docket":
		fmt.Println(name)
	case "docker":
		fmt.Println(name)
	case "dockec":
		fmt.Println(name)
	default:
		fmt.Println(name)
	}

}
